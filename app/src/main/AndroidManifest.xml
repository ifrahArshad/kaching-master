<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.devclear.kaching">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FullScreenTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ForgotPasswordActivity"></activity>
        <activity android:name=".FollowedList" />
        <activity
            android:name=".EditTagsActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".EditImagesActivity"
            android:theme="@style/FullScreenTheme" />
        <activity android:name=".ViewProfile" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/FullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SuccessfulSignUp"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/FullScreenTheme" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>